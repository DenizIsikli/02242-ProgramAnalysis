# Week 1 — Getting Started §3

## Improve the Regex-based Analysis (`syntactic-regex`)

- File changed: `solutions/syntactic/src/syntactic_regex.py`
- Added checks for `divide by zero` (looks for `/`), `null pointer` (looks for `null`), and `*` (looks for a `while` loop), alongside the original `assertion error` check.
- Verified with `jpamb -vv analyse syntactic-regex`.
